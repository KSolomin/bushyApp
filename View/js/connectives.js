/*connectives.js*/
/*global $, bushApp*/

bushApp.influx = {
    create:         function () {
        'use strict';
        var influx = bushApp.element.create('influx');
        return influx;
    }
};

bushApp.furcation = {
    create: function () {
        'use strict';

        var furc = bushApp.element.create('furcation');
        return furc;
    }
};

bushApp.conflux = {
    create: function () {
        'use strict';

        var conflux = bushApp.element.create('conflux');
        return conflux;
    }
};